let sports = ["Badminton", "Cricket", "Football", "VolleyBall", "BasketBall"];

let firstSport = sports[0];
let lastSport = sports[sports.length - 1];

console.log(`First sport is ${firstSport} and the last sport is ${lastSport}.`);
